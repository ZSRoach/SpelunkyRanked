[project]
name = "S2RankedBridge"
version = "1.20"
description = "Bridge for S2 Ranked"
readme = "../README.md"
requires-python = ">=3.10"
dependencies = [
    "PySide6",
    "python-socketio[client]",
    "requests",
    "pywin32",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
